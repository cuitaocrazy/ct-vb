# 个人单词本 (ct-vb)

项目上下文、技术栈和开发规则见 [openspec/config.yaml](openspec/config.yaml)。

## 常用命令

```bash
pnpm dev          # 启动开发服务器
pnpm build        # 构建
pnpm lint         # ESLint 检查
pnpm test         # Vitest 单元/集成测试
pnpm test:bdd     # Cucumber + Playwright 验收测试
```
